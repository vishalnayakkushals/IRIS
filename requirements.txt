# Core data & image processing
pandas>=2.2,<3.0
numpy>=1.26,<2.0
Pillow>=10.4,<12.0

# HTTP (Google Drive sync + OpenAI Responses API calls)
requests>=2.32,<3.0

# Computer vision
opencv-python-headless>=4.10,<5.0
ultralytics>=8.3,<9.0

# Schema validation (pipeline config models)
pydantic>=2.0,<3.0

# Google Drive fallback downloader (large folder sync)
gdown>=5.2,<6.0

# Secret encryption at rest (secret_store.py)
cryptography>=43.0,<45.0
