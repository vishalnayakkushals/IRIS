# Python cache
__pycache__/
.pytest_cache/
*.pyc

# Virtual environments
.venv/
venv/

# Local runtime/artifacts
exports/
employee_assets/
data/*
!data/models/
data/models/*
!data/models/yolov8s.onnx
streamlit.out
streamlit.err

# Model weights — .pt stays local (PyTorch); .onnx committed so server gets it on git pull
*.pt

# Local datasets / snapshots
*.jpg
*.jpeg
*.png
*.bmp
*.webp

# CTO observer outputs (isolated and removable)
CTO/logs/*
CTO/reports/*
!CTO/logs/.gitkeep
!CTO/reports/.gitkeep

# Local launcher overrides (optional, local-only)
run-iris-validation.local.ps1
run-iris-normal.local.ps1
.local-secrets/

# Local env files (secrets)
.env
.env.local

# Root-level SQLite test DB (real DB lives in data/)
store_registry.db

# Frontend build artifacts and dependencies
frontend/node_modules/
frontend/dist/
frontend/build_err*.txt

# Runtime logs and PID files
deploy/no_docker/runtime_logs/

# Claude internal settings
.claude/settings.local.json

# Temporary repo-root file inventories
*temptracked_files.txt
