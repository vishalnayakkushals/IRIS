# Release Notes - 2026-03-02

## Feature Name
- Autonomous CTO Bot (self-checking run orchestration)

## What’s New
- Added `src/iris/cto_bot.py` to run role-based checks (QA, CTO, DevOps) every run.
- Added structured JSON log per run and append-only text logs for auditability.
- Added scheduled GitHub Actions workflow `.github/workflows/cto-bot.yml` to execute bot automatically every 30 minutes and on push.
- Added test coverage for bot log generation.

## Impact
- Improves reliability by continuously checking project health.
- Creates traceable run logs for each automated cycle.
- Reduces manual monitoring effort.

## Metrics / Monitoring
- KPI being tracked: pass/fail health status per run.
- Expected improvement: faster detection of build/test regressions.
- Dashboard / Tool used: GitHub Actions artifacts + bot JSON logs.
- Monitoring owner: Engineering.

## Availability
- Web / App / Both: Both (repo automation + code runtime).
- Release version (if applicable): main.

## Risks / Known Issues
- Current auto-remediation is conservative; some failures still require manual code fix.

## Rollback Plan
- Revert commit introducing `src/iris/cto_bot.py` and `.github/workflows/cto-bot.yml`.

## Validation
- `PYTHONPATH=src pytest -q` passed.
- `python src/iris/cto_bot.py --log-dir data/ops_logs_test` executed and generated logs.
